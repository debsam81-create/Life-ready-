{
  "name": "life-ready-academy",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "publish": "vite build && sudo sh -c 'lsof -t -iTCP:3000 -sTCP:LISTEN | xargs -r kill' && mkdir -p .run && setsid nohup npx serve -s dist -l 3000 > .run/server.log 2>&1 &"
  },
  "devDependencies": {
    "autoprefixer": "^10.5.0",
    "postcss": "^8.5.15",
    "tailwindcss": "^3.4.19",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "@vitejs/plugin-react-swc": "^4.3.1",
    "lucide-react": "^1.21.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-router-dom": "^7.18.0"
  }
}
